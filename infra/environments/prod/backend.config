#   terraform init -backend-config=backend.config
# the first init. Per-environment `key` keeps state isolated across environments.
resource_group_name  = "raintech-tfstate-rg"
storage_account_name = "raintechtfstate"
container_name       = "tfstate"
key                  = "prod.terraform.tfstate"
